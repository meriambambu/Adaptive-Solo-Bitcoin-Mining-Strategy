# URL of the FastAPI backend (no trailing slash)
# In development the Vite proxy handles /api and /ws automatically,
# so this only needs to change for production deployments.
VITE_API_URL=http://localhost:8000
